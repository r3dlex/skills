# ADR 0001: Init (v3)

## Status
Accepted.

## Context
`r3dlex/skills` adopts the v3 AI-SDLC scaffold. Stack signal: skills/docs.

## Decision
Adopt `.ai/`, `.memory/`, and `docs/` layout from `r3dlex/skills/init-ai-repo/modules/documentation-blueprint.md`.

## Consequences
`topology_type=standalone`, `sync_strategy=physical-copy`. `.memory/human-override/` is terminal priority.
