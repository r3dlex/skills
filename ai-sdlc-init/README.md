# ai-sdlc-init compatibility alias

The canonical skill is [`init-ai-repo`](../init-ai-repo/SKILL.md).

This directory remains only so legacy skill loaders or prompts that reference `ai-sdlc-init` can redirect to the canonical skill. Do not add new implementation docs or modules here.
