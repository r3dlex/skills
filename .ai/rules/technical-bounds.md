# Technical Bounds (v3)
- Physical-copy is the canonical sync strategy.
- `.ai/matrix.json` is the single source of truth.
- Protected `main` requires PR-only delivery; non-admin auto-approval is disallowed.
- Admin self-approval is allowed only when the branch-policy checklist explicitly permits it and required checks still pass.
