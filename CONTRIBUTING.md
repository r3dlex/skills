# Contributing (v3)
See `AGENTS.md` and `.ai/matrix.json`. Protected `main` uses PR-only delivery. The merge gate requires architect + reviewer + executor agreement, all actionable PR comments resolved, and local plus host SCM CI green.
