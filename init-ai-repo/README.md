# init-ai-repo compatibility alias

The canonical skill is [`ai-catapult-init`](../ai-catapult-init/SKILL.md).

This directory remains only so legacy skill loaders or prompts that reference `init-ai-repo` can redirect to the canonical skill. Do not add new implementation docs or modules here.
