---
name: init-ai-repo
description: Deprecated compatibility alias for ai-catapult-init. Use only when legacy prompts invoke "init-ai-repo"; otherwise use "ai-catapult-init".
---

# Init AI Repo Compatibility Alias

`init-ai-repo` is deprecated. Load and run the canonical skill at `../ai-catapult-init/SKILL.md`.

Preserve legacy marker blocks named `init-ai-repo` in existing target repos unless an explicit marker migration is requested.
